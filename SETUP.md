# מדריך הקמה — קטלוג רכבי 7 מקומות

כל ההגדרות נמצאות בקובץ אחד: **`src/config.js`**

---

## שלב 1 — גיליון הרכבים (הקטלוג)

### 1.1 כותרות העמודות

צרו גיליון חדש ב-Google Sheets. **שורה 1** חייבת להכיל בדיוק את הכותרות הבאות
(אפשר בעברית או באנגלית — האתר מזהה את שתיהן):

| עברית | אנגלית | חובה? | הערות |
|---|---|---|---|
| `יצרן` | `make` | ✅ | טויוטה, מאזדה, קיה... |
| `דגם` | `model` | ✅ | |
| `שנה` | `year` | | 2019 |
| `קילומטראז'` | `km` | | מספר בלבד: 85000 |
| `יד` | `hand` | | מספר: 1, 2, 3 |
| `מחיר` | `price` | | מספר: 89000 (בלי ₪) |
| `תיבה` | `transmission` | | אוטומט / ידני |
| `דלק` | `fuel` | | בנזין / דיזל / היברידי |
| `צבע` | `color` | | |
| `מקומות` | `seats` | | ברירת מחדל: 7 |
| `תיאור` | `description` | | טקסט חופשי |
| `תמונות` | `images` | | קישורים מופרדים בפסיק |
| `סטטוס` | `status` | | `נמכר` מסתיר את הרכב מהאתר |

**חשוב:** מספרים (מחיר, ק"מ) — רק ספרות. האתר מנקה ₪ ופסיקים אוטומטית, אבל עדיף נקי.

**תמונות:** מדביקים כמה קישורים מופרדים בפסיק. אם משתמשים ב-Google Drive —
הדביקו את קישור השיתוף הרגיל, האתר ממיר אותו אוטומטית לתמונה ישירה.
(ודאו שההרשאה היא "כל מי שיש לו הקישור").

### 1.2 פרסום כ-CSV

1. בגיליון: **קובץ ← שיתוף ← פרסום באינטרנט**
2. בתפריט הראשון בחרו את **הגיליון** (לא "המסמך כולו")
3. בתפריט השני בחרו **ערכים מופרדים בפסיקים (.csv)**
4. לחצו **פרסם** ואשרו
5. העתיקו את הכתובת שמתקבלת — היא נראית כך:
   ```
   https://docs.google.com/spreadsheets/d/e/2PACX-1vQxxxxx/pub?gid=0&single=true&output=csv
   ```

### 1.3 הדבקה בקוד

בקובץ `src/config.js`, שורה 7:
```js
export const CARS_CSV_URL = 'כאן מדביקים את הכתובת'
```

> עדכון בגיליון מופיע באתר תוך כדקה (Google מרענן את ה-CSV).

---

## שלב 2 — גיליון הלידים (פניות מהאתר)

### 2.1 יצירת הגיליון

צרו גיליון **נפרד** (או לשונית נוספת) בשם `לידים`, עם הכותרות בשורה 1:

```
תאריך | שם | טלפון | רכב | מזהה רכב | מחיר | מקור
```

### 2.2 קוד ה-Apps Script

בגיליון הלידים: **תוספים ← Apps Script**. מחקו את כל מה שיש והדביקו:

```javascript
function doPost(e) {
  try {
    var data  = JSON.parse(e.postData.contents);
    var sheet = SpreadsheetApp.getActiveSpreadsheet().getSheetByName('לידים')
             || SpreadsheetApp.getActiveSpreadsheet().getSheets()[0];

    sheet.appendRow([
      new Date(),
      data.name  || '',
      "'" + (data.phone || ''),   // הגרש שומר על אפס מוביל
      data.car   || '',
      data.carId || '',
      data.price || '',
      data.source || 'אתר'
    ]);

    return ContentService
      .createTextOutput(JSON.stringify({ ok: true }))
      .setMimeType(ContentService.MimeType.JSON);

  } catch (err) {
    return ContentService
      .createTextOutput(JSON.stringify({ ok: false, error: String(err) }))
      .setMimeType(ContentService.MimeType.JSON);
  }
}

function doGet() {
  return ContentService.createTextOutput('הסקריפט פעיל ✓');
}
```

### 2.3 פרסום כ-Web App

1. שמרו (💾)
2. לחצו **פריסה ← פריסה חדשה**
3. לחצו על גלגל השיניים ⚙️ ליד "בחר סוג" ← **אפליקציית אינטרנט**
4. הגדירו:
   - **הפעל בתור:** אני (הכתובת שלך)
   - **למי יש גישה:** **כל אחד** ← ⚠️ חובה, אחרת האתר לא יוכל לשלוח
5. לחצו **פריסה** ← **אשר גישה** ← בחרו את החשבון שלכם
6. אם מופיעה אזהרה: **מתקדם ← המשך אל (שם הפרויקט)**
7. העתיקו את **כתובת אפליקציית האינטרנט**:
   ```
   https://script.google.com/macros/s/AKfycbxxxxx/exec
   ```

### 2.4 הדבקה בקוד

בקובץ `src/config.js`, שורה 11:
```js
export const LEADS_WEBAPP_URL = 'כאן מדביקים את כתובת ה-exec'
```

> **בדיקה:** פתחו את כתובת ה-exec בדפדפן — אמור להופיע "הסקריפט פעיל ✓".

> **שימו לב:** כל שינוי בקוד הסקריפט דורש **פריסה ← נהל פריסות ← עריכה ← גרסה: חדשה**,
> אחרת השינוי לא נכנס לתוקף.

---

## שלב 3 — מספר הוואטסאפ

בקובץ `src/config.js`, שורה 16 — פורמט בינלאומי בלי `+` ובלי אפס מוביל:

```js
export const WHATSAPP_NUMBER = '972501234567'   // עבור 050-1234567
```

וכן פרטי העסק בהמשך הקובץ (שם, סלוגן, טלפון לתצוגה).

---

## איך זה עובד

| פעולה | מה קורה |
|---|---|
| מבקר נכנס לאתר | האתר מושך את ה-CSV מהגיליון ומציג את הרכבים |
| לוחץ על רכב | דף רכב מלא — גלריה, מפרט, תיאור |
| לוחץ "לפרטים נוספים" | נפתח טופס קצר: שם + טלפון |
| שולח | הליד נשמר בגיליון ← נפתח וואטסאפ עם הודעה מוכנה |

---

## הרצה מקומית

```bash
npm install
npm run dev      # פיתוח
npm run build    # בנייה לפרודקשן
```

הפריסה ל-Vercel אוטומטית בכל `git push`.
